apply plugin: 'com.android.application'

android {

    signingConfigs {
        config {
            keyAlias 'PG Android Key'
            keyPassword 'android47jks'
            storeFile file('/home/paul/local/personal/usefull-information/keystores/android-apk-key.jks')
            storePassword 'android47jks'
        }
    }
    compileSdkVersion 23
    buildToolsVersion "23.0.3"
    defaultConfig {
        applicationId "net.manhica.dss.explorer"
        minSdkVersion 19
        targetSdkVersion 19
        versionCode 8
        versionName "1.0"
        archivesBaseName = "$applicationName-v$versionName"
    }
    buildTypes {
        release {
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
            zipAlignEnabled true
            signingConfig signingConfigs.config
        }
        debug {
            zipAlignEnabled true
        }
    }
    productFlavors {
    }
}

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    testCompile 'junit:junit:4.12'
    compile project(path: ':mapsme-lib')
    compile 'com.android.support:appcompat-v7:23.4.0'
    compile 'com.android.support:design:23.4.0'
    compile 'com.android.support:support-v4:23.4.0'
    compile 'org.jdom:jdom2:2.0.6'
}
